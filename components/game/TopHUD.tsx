// TopHUD — minimal overlay (flee countdown is drawn on canvas).
// Player/enemy info is now in BottomControls.

import React from 'react';

interface Props {
    // No props needed — all info moved to BottomControls
}

const TopHUD: React.FC<Props> = () => {
    return null;
};

export default TopHUD;
